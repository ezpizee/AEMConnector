<div class="mt-3">
    <div class="alert alert-{{#if type}}{{type}}{{else}}info{{/if}}">
        {{#if message}}
            {{{message}}}
        {{else}}
            Alert Error. Message is required, but missing.
        {{/if}}
        <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
    </div>
</div>